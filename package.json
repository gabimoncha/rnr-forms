{
  "name": "@rnr-forms/monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:docs": "turbo dev --filter=@rnr-forms/docs",
    "dev:showcase": "turbo dev --filter=@rnr-forms/showcase --concurrency 32",
    "lint": "turbo lint",
    "test": "turbo test",
    "build": "turbo build",
    "build:docs": "turbo build --filter=@rnr-forms/docs",
    "build:showcase": "turbo build --filter=@rnr-forms/showcase",
    "registry": "turbo registry",
    "format": "prettier --write .",
    "clean": "turbo clean && rm -rf .turbo node_modules"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "turbo": "^2.5.5",
    "typescript": "^5.8.3"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@babel/*",
        "expo-modules-*",
        "typescript"
      ]
    }
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "packageManager": "pnpm@10.14.0",
  "resolutions": {
    "lightningcss": "1.27.0"
  }
}
